function EI = histeq_gray(I)
[row, col, ~] = size(I);
EI = zeros(row, col);
for i = 1:row
    for j = 1:col
        count(1, I(i, j) + 1) = count(1, I(i, j) + 1) + 1;
    end
end
%% fill the following section to equalize the historam of I, put the result in EI

%%

I = imread('d:/git/data/asset/gray.jpg');
[row, col, ~] = size(I);
histogram = zeros(1, 256);
for i = 1:row
    for j = 1:col
        histogram(1, I(i, j) + 1) = histogram(1, I(i, j) +) + 1;
    end
end
histogram

